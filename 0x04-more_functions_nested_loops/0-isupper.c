#include "main.h"

/**
 * Function - Print that checks for uppercase
 *   return 1 if c is uppercase and 0 otherwise
 *
 * Return: Always 1.
 */

int _isupper(int c)
{
	 if (c >= 'A' && c <= 'Z')
	 {
		  return (0);
	} 		  
	 else
	   {
		     return (0); 
	   }
}
